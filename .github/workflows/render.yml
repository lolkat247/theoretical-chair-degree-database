name: Render LaTeX

on: push

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Export EERD
        run: |
          docker run --rm -v "$PWD/latex:/data" rlespinasse/drawio-export:latest -f pdf
          ls -la latex/export/
          mv latex/export/eerd-EERD.pdf latex/eerd.pdf || mv latex/export/eerd*.pdf latex/eerd.pdf
          ls -la latex/eerd.pdf

      - name: Compile LaTeX
        uses: xu-cheng/latex-action@v3
        with:
          root_file: main.tex
          working_directory: latex

      - name: Push PDF to build branch
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git checkout --orphan build
          git rm -rf .
          cp latex/main.pdf proposal.pdf
          git add proposal.pdf
          git commit -m "Render proposal.pdf from ${{ github.sha }}"
          git push origin build --force
